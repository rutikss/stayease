<% if(success && success.length > 0){ %>
<div class="alert alert-success alert-dismissible fade show mt-3 text-center mx-auto" style="max-width: 560px;">
    <i class="fa-solid fa-circle-check me-2"></i><%= success %>
    <button class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
</div>
<% } %>

<% if(error && error.length > 0){ %>
<div class="alert alert-danger alert-dismissible fade show mt-3 text-center mx-auto" style="max-width: 560px;">
    <i class="fa-solid fa-circle-exclamation me-2"></i><%= error %>
    <button class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
</div>
<% } %>
